url: ~
template:
  bootstrap: 5
  bslib:
    base_font: {google: "Mulish"}
    heading_font: {google: "Mulish"}
    code_font: {google: "Fira Code"}
    primary: "#0B7189"

navbar:
  structure:
    left:  [intro, articles, reference, news]
    right: [search, github]

reference:
  - title: Polars-specific functions
    desc: |
      Functionalities from Polars
    contents:
      - as_polars
      - describe
      - describe_plan
      - describe_optimized_plan
      - fetch
      - make_unique_id
      - sink_csv
      - sink_parquet
      - to_r
  - title: From `dplyr`
    desc: |
      Functions coming from `dplyr`
    contents:
      - add_count.DataFrame
      - anti_join.DataFrame
      - arrange.DataFrame
      - bind_cols_polars
      - bind_rows_polars
      - collect.LazyFrame
      - count.DataFrame
      - cross_join.DataFrame
      - distinct.DataFrame
      - filter.DataFrame
      - full_join.DataFrame
      - group_by.DataFrame
      - inner_join.DataFrame
      - left_join.DataFrame
      - mutate.DataFrame
      - pull.DataFrame
      - relocate.DataFrame
      - rename.DataFrame
      - rename_with.DataFrame
      - right_join.DataFrame
      - select.DataFrame
      - semi_join.DataFrame
      - separate.DataFrame
      - slice_head.DataFrame
      - slice_tail.DataFrame
      - slice_sample.DataFrame
      - summarize.DataFrame
      - ungroup.DataFrame
  - title: From `tidyr`
    desc: |
      Functions coming from `tidyr`
    contents:
      - complete.DataFrame
      - drop_na.DataFrame
      - fill.DataFrame
      - pivot_longer.DataFrame
      - pivot_wider.DataFrame
      - replace_na.DataFrame
      - separate.DataFrame
      - unite.DataFrame
