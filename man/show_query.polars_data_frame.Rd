% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/show_query.R
\name{show_query.polars_data_frame}
\alias{show_query.polars_data_frame}
\alias{show_query.polars_lazy_frame}
\title{Show the polars code equivalent to the tidypolars pipeline}
\usage{
\method{show_query}{polars_data_frame}(x, ...)

\method{show_query}{polars_lazy_frame}(x, ...)
}
\arguments{
\item{x}{A Polars Data/LazyFrame that went through \code{tidypolars} functions.}

\item{...}{Not used.}
}
\value{
The input, invisibly. This function is called for its side effect
of printing the polars query.
}
\description{
\code{show_query()} prints the pure polars code that \code{tidypolars} runs in the
background. This code can be copy-pasted and run with \code{polars} only (the
input data must be assigned to the name displayed in the first line of the
query).

Recording the query is enabled by default. It can be disabled with
\code{options(tidypolars_record_query = FALSE)}, in which case \code{show_query()}
errors (see \link{tidypolars_options}). Objects created while recording was
disabled cannot show their query.

Some values cannot be displayed in a copy-pasteable way, for example long
vectors coming from the calling environment (those are truncated and shown
as a placeholder like \code{`<numeric of length 200>`}).
}
\examples{
\dontshow{if (require("dplyr", quietly = TRUE)) withAutoprint(\{ # examplesIf}
mtcars |>
  as_polars_lf() |>
  filter(cyl == 4) |>
  mutate(mpg2 = mpg * 2, .by = am) |>
  show_query()
\dontshow{\}) # examplesIf}
}
