% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/show_query.R
\name{show_query.polars_data_frame}
\alias{show_query.polars_data_frame}
\alias{show_query.polars_lazy_frame}
\title{Show the polars code equivalent to the tidypolars pipeline}
\usage{
\method{show_query}{polars_data_frame}(x, ...)

\method{show_query}{polars_lazy_frame}(x, ...)
}
\arguments{
\item{x}{A Polars Data/LazyFrame that went through \code{tidypolars} functions.}

\item{...}{Not used.}
}
\value{
The input, invisibly. This function is called for its side effect
of printing the polars query.
}
\description{
\code{show_query()} prints the pure \code{polars} code that \code{tidypolars} runs in the
background. This code can be copy-pasted and run with \code{polars} only (the
input data must be assigned to the name displayed in the first line of the
query).

Recording the query is \strong{disabled} by default, meaning that \code{show_query()} will
error with an informative message. It can be enabled with
\code{options(tidypolars_record_query = TRUE)} (see \link{tidypolars_options}).

To keep the output readable and close to hand-written \code{polars} code, R
operators are used instead of their method equivalent (e.g. \code{a$add(b)} is
shown as \code{a + b}), and user-defined functions are displayed as a call
rather than expanded into the operations they perform internally.

Long values coming from the calling environment are referred to by the code
they come from -- the name of the object holding them, or the call that
produced them, e.g. \code{pl$lit(runif(200))} -- so that the printed code stays
copy-pasteable. Values that are too long to display and whose source code
cannot be recovered are shown as a placeholder like
\code{`<numeric of length 200>`}.
}
\examples{
\dontshow{if (require("dplyr", quietly = TRUE) & require("withr", quietly = TRUE)) withAutoprint(\{ # examplesIf}
withr::with_options(
  list(tidypolars_record_query = TRUE),
  mtcars |>
    as_polars_lf() |>
    filter(cyl == 4) |>
    mutate(
      mpg2 = mpg * 2,
      mpg2_max = max(mpg2),
     .by = am
    ) |>
    show_query()
)
\dontshow{\}) # examplesIf}
}
