# deprecated arguments in scan/read_csv_polars

    Code
      x <- scan_csv_polars(dest, rechunk = TRUE)
    Condition
      Warning:
      The `rechunk` argument of `scan_csv_polars()` is deprecated as of tidypolars 0.20.0.
      i This argument has no replacement.
      Warning:
      The `rechunk` argument of `scan_csv_polars()` is deprecated as of tidypolars 0.20.0.
      i This argument has no replacement.

---

    Code
      x <- read_csv_polars(dest, rechunk = TRUE)
    Condition
      Warning:
      The `rechunk` argument of `read_csv_polars()` is deprecated as of tidypolars 0.20.0.
      i This argument has no replacement.
      Warning:
      The `rechunk` argument of `read_csv_polars()` is deprecated as of tidypolars 0.20.0.
      i This argument has no replacement.

# deprecated arguments in scan/read_ipc_polars

    Code
      new_dat <- scan_ipc_polars(dest, rechunk = TRUE)
    Condition
      Warning:
      The `rechunk` argument of `scan_ipc_polars()` is deprecated as of tidypolars 0.20.0.
      i This argument has no replacement.
      Warning:
      The `rechunk` argument of `scan_ipc_polars()` is deprecated as of tidypolars 0.20.0.
      i This argument has no replacement.

---

    Code
      new_dat <- read_ipc_polars(dest, rechunk = TRUE)
    Condition
      Warning:
      The `rechunk` argument of `read_ipc_polars()` is deprecated as of tidypolars 0.20.0.
      i This argument has no replacement.
      Warning:
      The `rechunk` argument of `read_ipc_polars()` is deprecated as of tidypolars 0.20.0.
      i This argument has no replacement.

# deprecated arguments in scan/read_ndjson_polars

    Code
      new_dat <- scan_ndjson_polars(dest, rechunk = TRUE)
    Condition
      Warning:
      The `rechunk` argument of `scan_ndjson_polars()` is deprecated as of tidypolars 0.20.0.
      i This argument has no replacement.
      Warning:
      The `rechunk` argument of `scan_ndjson_polars()` is deprecated as of tidypolars 0.20.0.
      i This argument has no replacement.

---

    Code
      new_dat <- read_ndjson_polars(dest, rechunk = TRUE)
    Condition
      Warning:
      The `rechunk` argument of `read_ndjson_polars()` is deprecated as of tidypolars 0.20.0.
      i This argument has no replacement.
      Warning:
      The `rechunk` argument of `read_ndjson_polars()` is deprecated as of tidypolars 0.20.0.
      i This argument has no replacement.

# deprecated arguments in scan/read_parquet_polars

    Code
      new_dat <- scan_parquet_polars(dest, rechunk = TRUE)
    Condition
      Warning:
      The `rechunk` argument of `scan_parquet_polars()` is deprecated as of tidypolars 0.20.0.
      i This argument has no replacement.

---

    Code
      new_dat <- read_parquet_polars(dest, rechunk = TRUE)
    Condition
      Warning:
      The `rechunk` argument of `read_parquet_polars()` is deprecated as of tidypolars 0.20.0.
      i This argument has no replacement.

