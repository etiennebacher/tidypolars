# error when different types

    Code
      compute(current)
    Condition
      Error in `compute()`:
      ! cannot compare string with numeric type (f64)

---

    Code
      compute(current)
    Condition
      Error in `compute()`:
      ! cannot compare string with numeric type (f64)

