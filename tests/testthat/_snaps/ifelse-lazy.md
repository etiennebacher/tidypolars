# error when different types

    Code
      collect(current)
    Condition
      Error in `collect()`:
      ! cannot compare string with numeric type (f64)

---

    Code
      collect(current)
    Condition
      Error in `collect()`:
      ! cannot compare string with numeric type (f64)

